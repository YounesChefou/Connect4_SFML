cmake_minimum_required(VERSION 3.20) 
project(connect4) # Put C,C++ or anything in place of XX 

# set(FLAGS 
#     -Wall 
#     -g
#     ) # Specify flags and libraries of corresponding language in place of FLAGSANDLIBRARIES


add_subdirectory(src)